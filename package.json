{
  "name": "publishing-connector",
  "source": "./src/index.ts",
  "version": "0.0.1",
  "description": "A connector to automate publishing in Ocean Protocol ecosystems",
  "main": "index.js",
  "scripts": {
    "build": "tsc || exit 0",
    "cleanWin": "rmdir dist /s /q",
    "clean": "rm -rf ./dist/",
    "start": "npm run cleanWin && npm run build && node ./dist/src/index.js",
    "pretest": "npm run build",
    "test": "npx mocha -- ./dist/test/**/*.test.js"
  },
  "author": "Fabian Gast",
  "license": "Apache-2.0",
  "dependencies": {
    "@deltadao/nautilus": "^0.3.0-beta.2",
    "dotenv": "^16.3.1",
    "ethers": "^5.7.2",
    "nats": "^2.16.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.5",
    "@types/mocha": "^10.0.1",
    "chai": "^4.3.8",
    "mocha": "^10.2.0",
    "typescript": "^5.1.6"
  }
}
